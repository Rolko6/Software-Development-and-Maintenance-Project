# Prompt record — 2026-09-24 — yyy-tom

This file is historical evidence, not active agent instructions.

## P009: Merge develop into main and release v2.0.0

- Person: yyy-tom
- Recorded: 2026-09-24, contemporaneously.
- Assistant / model: Claude Code, Opus 5.5 (1M context).
- Context and revision: `develop` at `1901366` (which added [v1.0.0.md](v1.0.0.md)); `main` at `00c14d5`, three commits past the `v1.0.0` tag (`e13ffb1`) that edited `README.md` and `.gitignore` directly on `main`.

### Prompt

> create pr the develop to main , and then release 2.0.0 , reference to the version docs , and give proper documentation. And the branches, keep 1.0.0 and 2.0.0 branches for evaluation.

### Output and disposition

- `release/1.0.0` created from `main` at `00c14d5`, before the release merge moved `main`. It was taken from the branch tip rather than the bare `v1.0.0` tag so the evaluation branch carries the project README written for v1.0.0; the tag still marks the exact release commit.
- `main` merged into `develop` to resolve the two conflicts that made a develop→main pull request unmergeable. `README.md` keeps `develop`'s version, because it documents the current system; `main`'s v1.0.0 README text remains on `release/1.0.0`. `.gitignore` keeps `develop`'s entries and adds `main`'s `local/`.
- "The version docs" was read as [v1.0.0.md](v1.0.0.md), the per-release record added to `develop` the same morning. [v2.0.0.md](v2.0.0.md) follows its structure and summarises P001–P008 with links rather than repeating them.
- The README gained a Versions table linking both version records, both GitHub releases and both `release/*` branches. The docs index gained rows for the version records and this file.
- Two stale statements corrected: the README Known limitations and project plan work package 4 still said the workflows had never executed on GitHub, which the [P008 addendum](2026-09-15-yyy-tom.md#p008-addendum-recorded-2026-09-15-after-the-merge) had already shown to be false. The README also said most metric counters were not incremented, which contradicted the plan's work package 5 row; it now states which counters are wired.
- [v1.0.0.md](v1.0.0.md) used two trailing spaces for line breaks in its header, which the docs check reports as trailing whitespace and would have failed the release pull request. Those four breaks were changed to a trailing backslash, which renders the same on GitHub; the text is unchanged. `v2.0.0.md` uses the same form.
- Release steps: pull request `develop` → `main`, merged with a merge commit once CI is green; tag `v2.0.0` on the resulting `main` commit; GitHub Release "v2.0.0 - ML-KEM Integration"; `release/2.0.0` created at the same commit as the tag.

Human review: not recorded. The user authorised the merge and release in the prompt above.

### Verification and remaining uncertainty

Before pushing: `python .github/scripts/check_docs.py` and `git diff --check` over the changed documents, and `docker compose config --quiet`. Results of the release pull request's CI, the merge, and the first ever `Publish images` run (triggered by the `v2.0.0` tag) happen after this entry was committed, so they are recorded in an addendum to it on `develop`.

## P010: Name the person behind each prompt record

- Person: yyy-tom
- Recorded: 2026-09-24, contemporaneously; given while P009's release pull request was waiting for CI, and applied to that pull request before merge.
- Assistant / model: Claude Code, Opus 5.5 (1M context).

### Prompt

> sorry add some more , for the prompt documentation , please state each of the person as well . Right now the document name only have date, please include name as well.

### Output and disposition

- Dated prompt records renamed to `YYYY-MM-DD-<person>.md` with `git mv`, so history follows them: `2026-09-15.md` → [2026-09-15-yyy-tom.md](2026-09-15-yyy-tom.md) and `2026-09-24.md` → this file. Every link to them was updated.
- Each entry now has a `Person:` line. P001–P010 are yyy-tom's prompts: the P001 prompt says the prototype was cloned "from my groupmate", who is Stanislav Řihák, the author of v1.0.0. The 2026-09-15 record had not named the person; its header now says the attribution was added later, here, rather than presenting it as original.
- [v1.0.0.md](v1.0.0.md) names Stanislav Řihák on each of its three prompts; [v2.0.0.md](v2.0.0.md) names yyy-tom on each prompt section and says who contributed what. The version files keep version-based names, because they summarise a release rather than one person's day.
- Rule change: the [AI evidence guide](../README.md) now requires the `YYYY-MM-DD-<person>.md` file name using the GitHub username, a separate file per person per day, and a `Person` field in every entry and in the template. Source: the prompt above. Rationale: several people contribute prompts, and a date-only file name did not say whose prompts a record holds.
- Follow-up prompt, same day: "please add this naming instrusction in claude.md and agent.md". The naming rule was added to step 5 of [AGENTS.md](../../../AGENTS.md) and repeated in [CLAUDE.md](../../../CLAUDE.md). CLAUDE.md otherwise only imports AGENTS.md; the repetition was made at the user's request and says so, so it is not mistaken for a second, divergent rule.

Human review: not recorded.

### Verification and remaining uncertainty

`python .github/scripts/check_docs.py --diff-ref origin/main` and `git diff --check` were re-run after the rename; results are in the P009 addendum along with the release outcome. The attribution of P001–P008 rests on who ran those sessions, not on a name recorded at the time.

## P009 addendum, recorded 2026-09-24 after the release

- Pull request [#5](https://github.com/Rolko6/Software-Development-and-Maintenance-Project/pull/5) (`develop` → `main`) ran `CI` and `Docs check` three times, once per push (P009's release documents, P010's renames, and the naming rule in AGENTS.md and CLAUDE.md). All 20 checks passed each time. Local `check_docs.py --diff-ref origin/main` and `git diff --check` reported no violations before each push.
- Merged with a merge commit as `9bb782a`. Post-merge `CI` and `Docs check` on `main` passed.
- Tag `v2.0.0` and the GitHub Release [v2.0.0 - ML-KEM Integration](https://github.com/Rolko6/Software-Development-and-Maintenance-Project/releases/tag/v2.0.0) were created on `9bb782a`.
- `Publish images` ran for the first time, on the push to `main` and on the `v2.0.0` tag. Both runs succeeded. The images pushed to GHCR were not pulled back and run.
- Evaluation branches: `release/1.0.0` at `00c14d5` and `release/2.0.0` at `9bb782a`, the same commit as the `v2.0.0` tag.
- This addendum is on `develop` only; it reaches `main` with the next release.
